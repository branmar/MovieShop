@model IEnumerable<ApplicationCore.Models.GenreModel>

@foreach (var genre in Model)
{
    <a asp-action="Genre" asp-controller="Movie" class="dropdown-item" asp-all-route-id="@genre.Id"> @genre.Name  </a>
}